{
  "stack": "react-typescript-vite",
  "packageManager": "npm",
  "framework": { "react": "19", "router": "react-router-dom 7", "styling": "tailwindcss 4", "bundler": "vite 7" },
  "build": {
    "INSTALL": "npm install",
    "TYPECHECK": "npx tsc --noEmit",
    "LINT": "npm run lint",
    "BUILD": "npm run build",
    "UNIT_TEST": "MANUAL: no test runner configured yet — CLAUDE.md plans Vitest + React Testing Library; until then verify with TYPECHECK + LINT and a manual dev-server check",
    "DEV_SERVER": "npm run dev",
    "PREVIEW": "npm run preview"
  },
  "changeTypeVerbs": {
    "ts|tsx": ["TYPECHECK", "LINT", "BUILD"],
    "css|tailwind": ["BUILD"],
    "package.json|lockfile": ["INSTALL", "BUILD"],
    "vite.config|tsconfig|eslint.config": ["TYPECHECK", "LINT", "BUILD"]
  },
  "jira": { "mode": "manual", "project": null },
  "_notes": [
    "The repo's CLAUDE.md is authoritative: functional components + named exports, components under the line limit, all backend calls through src/services/api.ts, typed responses from src/types/index.ts, loading + error states, showToast() feedback, Tailwind ordering with dark: variants.",
    "Backend for manual verification: SAP Commerce at VITE_API_URL (default https://localhost:9002, self-signed); vite dev server proxies /occ and /authorizationserver.",
    "When a test runner lands, replace UNIT_TEST with 'npx vitest run' and add it to the ts|tsx mapping.",
    "Set jira.mode to 'mcp' if the Atlassian MCP connector is available in your Claude Code session."
  ]
}
