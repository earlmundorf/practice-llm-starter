# Migration intake — {{PROJECT_NAME}}

> Generated by `sap-commerce-migrate-j21` skill on {{DATE}}.
> This document captures the inputs the skill needs before generating a plan. Auto-detected fields are populated from `scripts/detect_state.sh`. User-input fields are filled in via the skill's intake capture step (Phase 1 Step 3).
>
> **The skill does not generate a plan until this intake is signed off** (last section).

---

## Auto-detected state (from detect_state.sh)

| Field | Detected value |
|---|---|
| Project path | {{PROJECT_DIR}} |
| Current Commerce version | {{CURRENT_COMMERCE_VERSION}} |
| Current JDK pin | {{CURRENT_JDK}} |
| Current Spring (inferred) | {{CURRENT_SPRING}} |
| Custom extensions | {{CUSTOM_EXTENSIONS_LIST}} |
| OAuth footprint | {{OAUTH_FOOTPRINT}} |
| Deprecated extensions present | {{DEPRECATED_EXTENSIONS}} |
| Existing `bin/platform` extraction | {{PLATFORM_EXTRACTED}} |
| Suite ZIP in dependencies/ | {{SUITE_ZIP_STATUS}} |
| Integration pack ZIP in dependencies/ | {{INTPACK_ZIP_STATUS}} |
| `setantenv.sh` resolves | {{ANT_RESOLVES}} |
| Git branch | {{GIT_BRANCH}} |

**If any auto-detected field is missing or wrong**, fix `detect_state.sh` and re-run before continuing — don't paper over with manual entry. Auto-detection is the source of truth for environment state.

---

## User input — required (plan cannot generate without these)

### 1. Migration scope and target

#### 1.1 Target Commerce version

**Question:** Which `2211-jdk21.x` release are you migrating TO?

- **Default:** `2211-jdk21.9` (most recent confirmed under skill v0.4.x)
- **Format:** version string, e.g., `2211-jdk21.9`

**Your answer:** ____________________

### 2. Strategy

#### 2.1 In-place or copy-to-new-repo?

See `references/decision-tree.md` "Pre-branch decision: migration strategy" for the trade-off.

- **Default:** `in-place` if no separate target repo is named in 3.2 below; `copy` if one is.
- **Pick one:**
  - [ ] **in-place** — upgrade existing repo on a `migration/jdk21` branch
  - [ ] **copy** — `git archive HEAD` legacy into a fresh target repo

**Your answer:** ____________________

#### 2.2 Phase B execution path: sweeps or OpenRewrite?

See `references/decision-tree.md` "Branch 0".

- **Default:** `sweeps` for ≤5 custom extensions; user judgment for 5–15; `OpenRewrite` for 15+.
- **Pick one:**
  - [ ] sweeps (Claude-driven grep-and-edit)
  - [ ] OpenRewrite (SAP recipe, see `references/sap-notes/3618495-openrewrite-framework-update.md`)

**Your answer:** ____________________

### 3. Source materials and paths

#### 3.1 Path to extracted SAP distribution

**Question:** Where is the extracted SAP Commerce `2211-jdk21.x` distribution on your machine? Used to build the suite ZIP if not already in `core-customize/dependencies/`.

- **Skip if:** suite ZIP is already present (auto-detected above shows ✓).
- **Format:** absolute path, e.g., `/Users/me/development/sap-distros/CCL2211J2100U_9-80009731`

**Your answer:** ____________________

#### 3.2 Legacy project path (only if strategy is "copy")

**Question:** What is the absolute path to the legacy project repo to migrate FROM?

- **Skip if:** strategy is `in-place`.
- **Format:** absolute path

**Your answer:** ____________________

#### 3.3 New target project path (only if strategy is "copy")

**Question:** What is the absolute path to the new (clean) target project repo to migrate INTO?

- **Skip if:** strategy is `in-place`. Otherwise this is the cwd Claude Code is opened in.
- **Format:** absolute path

**Your answer:** ____________________

### 4. Data persistence story (load-bearing for Phase 0-prep safety net)

#### 4.1 Which scenario describes your environment?

This determines whether Step 0.0a (DB backup) and 0.0b (Solr export) are mandatory, conditional, or skipped. **Pick one:**

- [ ] **A. In-memory DB (HSQLDB)** — default Commerce dev config; no persistence. Step 0.0a is moot.
- [ ] **B. Persistent DB seeded by ImpEx** — dev with MySQL/HANA, no customer data. Backup = ImpEx files + extension git tag. Restore = re-run `yinitialize`.
- [ ] **C. Production with customer data + backup capability** — standard prod. Step 0.0a is **mandatory**: working `mysqldump` / RDS snapshot pipeline confirmed.
- [ ] **D. Production with customer data + NO backup capability** — ⛔ **STOP. Plan blocked.** Build backup pipeline before resuming. The migration will be unrecoverable on failure without it.
- [ ] **E. Session-only / no persistence** — carts/users in-memory only. Document expected loss; communicate maintenance window to users.

**Your answer:** Scenario ____ (one of A/B/C/D/E)

#### 4.2 Backup mechanism (only if scenario C)

**Question:** Describe the working backup mechanism you'll use for Step 0.0a.

- **Skip if:** scenario A, B, or E.
- **Halt if:** scenario D — return to 4.1 only after backup capability is built.
- **Format:** mechanism + verification commitment, e.g., `mysqldump --single-transaction; verify restorable on scratch instance before proceeding`.

**Your answer:** ____________________

#### 4.3 Solr backup or "rebuild from DB"

**Question:** How will you preserve the search index across migration?

- **Pick one:**
  - [ ] Snapshot/backup (`bin/solr backup`, filesystem snapshot, or equivalent)
  - [ ] Rebuild from DB via `solr_FullProductIndex` cronjob — document expected re-index time below

**Re-index time estimate (if rebuild):** ____________________ hours/minutes

**Your answer:** ____________________

### 5. Production characteristics (skip if scenario A, B, or E)

#### 5.1 OCC consumers list

**Question:** What apps/services consume your OCC API? (Required for Step 0.0c contract baseline capture.)

- **Format:** list of consumer apps + which OCC endpoints each uses, e.g., "spartacus-storefront: products/*, cart/*, users/{id}; native-mobile-app: products/search, cart/*"

**Your answer:** ____________________

#### 5.2 Cart quiesce decision

See `references/known-incidents.md` incident 6.

**Question:** How will you handle in-flight carts/sessions during the migration?

- **Pick one:**
  - [ ] **Drain** — stop new sessions, let existing ones complete or expire over an N-hour window. N: ____ hours.
  - [ ] **Accept loss** — accept that some carts won't deserialize. Expected loss percentage: ____ %.
  - [ ] **Maintenance window** — block all new sessions during cutover. Window length: ____ hours.

**Your answer:** ____________________

#### 5.3 Maintenance window length

**Question:** What's the maximum maintenance window you can take?

- **Format:** duration. Used for sizing Phase F's re-init / re-index activities.

**Your answer:** ____________________

### 6. Out-of-scope confirmations (auto-detected but worth confirming)

| Topic | Auto-detected | Confirm? |
|---|---|---|
| SmartEdit in use | {{SMARTEDIT_PRESENT}} | ☐ confirm / ☐ override (explain) |
| Custom Backoffice widgets | _user input required_ | ☐ none / ☐ yes (list extensions) |
| Custom Tomcat config | {{TOMCAT_CONFIG_PRESENT}} | ☐ confirm / ☐ override |
| OData / Olingo in use | _user input required_ | ☐ no / ☐ yes (list extensions) |
| SAML SSO configured | _user input required_ | ☐ no / ☐ yes (added complexity for OAuth phase) |

---

## Sign-off

The skill does not proceed past intake until this is signed off.

- [ ] All required fields (sections 1, 2, 3, 4) filled.
- [ ] If scenario D selected: plan blocked, no sign-off until Scenario D resolves to C.
- [ ] User approves the captured intake.

**Signed off by:** ____________________
**Date:** ____________________

---

## Plan-template substitution map

This section is for the skill's use. When generating `migration-docs/migration-plan.md` from `references/phase-guide.md`, substitute placeholders from intake fields:

| Placeholder | Source field |
|---|---|
| `{{PROJECT_DIR}}` | Auto-detected (cwd) |
| `{{PROJECT_NAME}}` | basename of `{{PROJECT_DIR}}` |
| `{{DATE}}` | Skill run date (YYYY-MM-DD) |
| `{{IN_PLACE | COPY}}` | 2.1 |
| `{{LEGACY_PROJECT_DIR}}` | 3.2 (only if copy) |
| `{{THIS_PROJECT_DIR}}` | 3.3 (only if copy) — else `{{PROJECT_DIR}}` |
| `{{SAP_DISTRO_EXTRACT_DIR}}` | 3.1 |
| `{{TARGET_COMMERCE_VERSION}}` | 1.1 |
| `{{PERSISTENCE_SCENARIO}}` | 4.1 (one of A/B/C/D/E) |
| `{{BACKUP_MECHANISM}}` | 4.2 |
| `{{SOLR_RECOVERY}}` | 4.3 |
| `{{OCC_CONSUMERS}}` | 5.1 |
| `{{CART_QUIESCE}}` | 5.2 |
