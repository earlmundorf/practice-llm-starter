# HAC Project Knowledge

Use this directory for project-specific HAC notes that should travel with this repository, such as known local context paths, approved diagnostic query patterns, or environment naming conventions.

Do not store passwords, cookies, session tokens, production endpoints, customer data, or local-only secrets here. Put credentials in an untracked `hac.env` file based on `hac.env.example`.
